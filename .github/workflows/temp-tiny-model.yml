name: Temporary Tiny Whisper Model
on:
  pull_request:
    types: [opened, synchronize, reopened]
permissions:
  contents: read
jobs:
  model:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download tiny English model
        run: |
          mkdir -p /tmp/package
          curl -L --fail --retry 4 --retry-delay 5 \
            -o /tmp/package/ggml-tiny.en-q5_1.bin \
            https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.en-q5_1.bin
          tar -C /tmp/package -czf /tmp/whisper-tiny-model.tar.gz .
      - name: Upload model
        uses: actions/upload-artifact@v4
        with:
          name: whisper-tiny-en-q5
          path: /tmp/whisper-tiny-model.tar.gz
          retention-days: 1
